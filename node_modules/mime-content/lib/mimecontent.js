// Generated by CoffeeScript 1.6.3
(function() {
  var html, json, mime, parse, urlEncoded, xml;

  xml = require('./xml');

  json = require('./json');

  urlEncoded = require('./urlencoded');

  html = require('./html');

  mime = function(contentType) {
    if (contentType == null) {
      contentType = '';
    }
    return contentType.toLowerCase().replace(/^\s+|\s+$/g, '').split(';')[0];
  };

  parse = function(content, contentType) {
    var mimeType;
    mimeType = mime(contentType);
    if (mimeType === 'application/xml' || mimeType === 'text/xml') {
      return xml(content);
    } else if (mimeType === 'application/json') {
      return json(content);
    } else if (mimeType === 'application/x-www-form-urlencoded') {
      return urlEncoded(content);
    } else if (mimeType === 'text/html') {
      return html(content);
    } else {
      return content;
    }
  };

  module.exports = parse;

}).call(this);
