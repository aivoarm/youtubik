// Generated by CoffeeScript 1.6.3
(function() {
  var parse, xmldom, xpath;

  xpath = require('xpath');

  xmldom = require('xmldom');

  parse = function(content) {
    var doc;
    doc = new xmldom.DOMParser().parseFromString(content);
    doc.xpath = function(selector, multi) {
      if (multi == null) {
        multi = false;
      }
      return xpath.select(selector, doc, !multi);
    };
    return doc;
  };

  module.exports = parse;

}).call(this);
